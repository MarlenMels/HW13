package PC.Keyboard;

public enum BackLight {
    YES("Yes, it has backlight"),
    NO("No, it has not backlight");
    private String isHas;
    BackLight(String isHAs) {
        this.isHas = isHas;
    }
}
